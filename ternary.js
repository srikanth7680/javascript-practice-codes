//ternary operator

let num = 9
result = num%2===0 ? "Even":"Odd"//? and : are used in ternary operator 
console.log(result);//odd

// condition is used before the ? mark and after that if condition resultant and after that using : else condition resultant
// This is called ternary operator

